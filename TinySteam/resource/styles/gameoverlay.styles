gameoverlay.styles
{
	include "resource/styles/steam.styles"
	
	colors 
	{
		Notifications.PanelPosition     "BottomRight"  // osx has toast in the upper right, but in-overlay should not.
	}
	
	styles 
	{
		detailsbox
		{
			render_bg
			{
				0="gradient( x0, y0, x1, y1, MindiWhite, MindiWhite )"
			}		
		}	
		
		detailsboxtitle
		{
			font-size=28
			font-style=uppercase
			textcolor="Mindi"
		}
	}
}